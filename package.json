{
  "name": "liz",
  "version": "0.1.0",
  "description": "and the blue bird",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-js": "webpack --config webpack.config.js",
    "watch-js": "webpack --watch",
    "build-sass": "node-sass src/scss/ -o build/css/",
    "watch-sass": "node-sass -w src/scss/ -o build/css/",
    "build": "npm run build-js && npm run build-sass",
    "watch": "npm run watch-js && npm run watch-sass"
  },
  "author": "dastardlylemon",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-loader": "^7.1.4",
    "babel-preset-env": "^1.7.0",
    "jquery": "^3.3.1",
    "node-sass": "^4.9.0",
    "underscore": "^1.9.1",
    "webpack": "^4.12.2",
    "webpack-cli": "^3.0.8"
  }
}
